apiVersion: apps/v1
kind: Deployment
metadata:
  name: hash-generator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hash-generator
  template:
    metadata:
      labels:
        app: hash-generator
    spec:
      containers:
        - name: hash-generator
          image: swipped/hash_generator:latest